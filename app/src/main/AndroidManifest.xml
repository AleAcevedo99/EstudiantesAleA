<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estudiantesale">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_school"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_school"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstudiantesAle">
        <activity android:name=".RecyclerActivity"></activity>
        <activity android:name=".EditActivity" />
        <activity android:name=".ListEditDeleteActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>